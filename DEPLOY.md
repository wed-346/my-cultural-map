# Инструкция по деплою на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Назовите репозиторий (например: `my-cultural-map`)
4. Создайте репозиторий (можно оставить пустым)

## Шаг 2: Подготовьте проект

1. Откройте `package.json`
2. Замените `YOUR_USERNAME` в строке `"homepage"` на ваш GitHub username
   Например, если ваш username `denis123`, то должно быть:
   ```json
   "homepage": "https://denis123.github.io/my-cultural-map"
   ```

## Шаг 3: Установите gh-pages

Откройте терминал в папке `my-react-app` и выполните:

```bash
npm install --save-dev gh-pages
```

## Шаг 4: Инициализируйте Git (если еще не сделано)

```bash
cd my-react-app
git init
git add .
git commit -m "Initial commit"
```

## Шаг 5: Подключите к GitHub репозиторию

```bash
git remote add origin https://github.com/wed-346/my-cultural-map
git branch -M main
git push -u origin main
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

## Шаг 6: Задеплойте на GitHub Pages

```bash
npm run deploy
```

Это создаст ветку `gh-pages` и задеплоит ваш сайт.

## Шаг 7: Включите GitHub Pages

1. Перейдите в настройки вашего репозитория на GitHub
2. В разделе "Pages" выберите источник: `gh-pages` branch
3. Сохраните

## Готово! 🎉

Ваш сайт будет доступен по адресу:
`https://YOUR_USERNAME.github.io/my-cultural-map`

## Обновление сайта

После любых изменений в коде:

```bash
git add .
git commit -m "Update"
git push
npm run deploy
```

## Важно!

- Убедитесь, что в `package.json` правильно указан `homepage` с вашим username
- После первого деплоя подождите 1-2 минуты, пока GitHub Pages обновится
- Если что-то не работает, проверьте настройки Pages в репозитории

